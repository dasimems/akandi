import SectionContainer from "@/components/layouts/SectionContainer";
import React from "react";

const CarList = () => {
  return (
    <SectionContainer className="py-10 md:py-20">
      this is going to be linked when api is approved
    </SectionContainer>
  );
};

export default CarList;
